/**
 * 默认登录路由（固定值）。
 *
 * 安全路由（自定义登录入口）启用后，访问首页只会落到这里并提示「路由错误」，
 * 不会自动跳转到安全路由——安全路由必须由用户手动输入完整地址。
 * 服务端不再下发安全路由的值，前端通过 /api/route-check 按「当前路径」询问是否可展示登录页。
 */
export const DEFAULT_LOGIN_ROUTE = '/login'
